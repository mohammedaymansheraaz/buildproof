"use client";

import { UnifiedAuditFilm } from "@/components/unified-audit-film";

/** The public home now has one continuous audit-film narrative. */
export function LandingPage() {
  return <UnifiedAuditFilm />;
}
